{
  "buildCommand": "cd salapro-app && npm install && npm run build",
  "outputDirectory": "salapro-app/dist",
  "installCommand": "echo skip",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
